const Event = use("Event");
const Mail = use("Mail");

// Event.on('user::created', async (user) => {
//   await Mail.send('new.user', user, (message) => {
//     message.to(user.email)
//     message.from('from@email')
//   })
// })

// Event.on("user::created", "UserController.registered");
